# presistand md file
- create a presistant md file wich higlights important items from the coversation this is a tool which agent could call mid convo

# Summary Buffer Window (or Rolling Summary Memory)
- a memory window that after like 15 messages summarizes tham and reinject into a fresh convo with all the context 

# tool shortning
- set max output of a file so log files dont overwelm it 
- add a seperate sumarization tool that reads bigg files and shortens the output